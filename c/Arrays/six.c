#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 * description - practice
 * int main(void) - printfs code
 * Return - always 0
 * int main - printfs the code and retturns to 0
 */
int main(void)
{
	char m[] = "when i am \"45\"\t i would have accomplished alot";
	{
		printf("%ld\n", strlen(m));
	}
	return (0);
}
